import {conversation} from './connected.js';
import {reviewSummary,reviewFor} from './review-model.js';
import {requestWorkspace} from './requests.js';
import {BOARDS,TYPES,PEOPLE,stageFor,transitionsFor} from './data.js';
import {store,sla} from './store.js';
import {esc,icon,avatar,badge,priority,statusBadge,typeIcon,btn,ib,date,money} from './ui.js';
import {documentWorkspace} from './documents.js';
import {workflow} from './workflow.js';
export const detail={key:null,tab:'workspace',activity:'all',page:1,zoom:false,queue:[],commentDrafts:{},reviewAction:null,reviewEdit:null,focusField:null};
export const currentTask=()=>store.tasks.find(t=>t.key===detail.key);
export function openTask(key,queue){detail.key=key;detail.reviewAction=null;detail.reviewEdit=null;detail.focusField=null;detail.tab='workspace';detail.page=1;detail.zoom=false;detail.activity='all';detail.queue=queue||store.tasks.filter(t=>t.board===currentTask().board).map(t=>t.key);renderDetail();const dialog=document.querySelector('#task-dialog');if(!dialog.open)dialog.showModal();document.querySelector('#task-title').focus();}
export function closeTask(){document.querySelector('#task-dialog').close();const old=detail.key;detail.key=null;document.querySelector(`[data-task="${old}"]`)?.focus();}
function actions(t){if(t.type==='document')return '';return transitionsFor(t).map(a=>`<button class="btn ${a.to==='completed'?'success':a.to==='correction'?'correction-btn':a.to==='resubmitted'?'primary':''}" data-action="transition:${a.to}">${a.to==='completed'?icon('check',15):a.to==='resubmitted'?icon('upload',15):''}${a.label}</button>`).join('');}
export function renderDetail(){const t=currentTask();if(!t)return;const dialog=document.querySelector('#task-dialog'),b=BOARDS[t.board],s=stageFor(t),position=detail.queue.indexOf(t.key);dialog.dataset.type=t.type;dialog.dataset.tab=detail.tab;const tableScroll=dialog.querySelector('#review-table-scroll')?.scrollTop||0,pdfScroll=dialog.querySelector('#pdf-scroll')?.scrollTop||0;const scroll=dialog.querySelector('.detail-main')?.scrollTop||0,contextScroll=dialog.querySelector('.context-panel')?.scrollTop||0;
 dialog.innerHTML=`<div class="detail-topline"><div class="detail-breadcrumb">${typeIcon(t.type)}<span>${b.space}</span>${icon('chevron',10)}<strong>${t.key}</strong>${ib('copy','Copy task link','copy-task')}</div><div class="detail-navigation"><span>${position+1} of ${detail.queue.length}</span><button class="icon-btn" aria-label="Previous task" data-action="previous-task" ${position<=0?'disabled':''}>${icon('back',17)}</button><button class="icon-btn" aria-label="Next task" data-action="next-task" ${position>=detail.queue.length-1?'disabled':''}>${icon('arrow',17)}</button><span class="vertical-divider"></span>${ib('close','Close task','close-task')}</div></div><header class="detail-header"><h1 id="task-title" tabindex="-1">${esc(t.title)}</h1><div class="detail-header-bottom"><div class="detail-meta">${statusBadge(t)}${priority(t)}<span class="meta-student">${icon('profile',14)}${esc(t.student)}</span>${t.type==='document'&&['clean','exceptions'].includes(t.status)?badge(reviewSummary(t).pending.length?reviewSummary(t).pending.length+' checks require review':reviewSummary(t).empty.length?reviewSummary(t).empty.length+' values required':'Checks resolved',reviewSummary(t).pending.length?'amber':'green'):''}</div><div class="detail-actions">${t.type==='document'&&['clean','exceptions'].includes(t.status)?btn(t.escalated?'Escalated to registrar':'Escalate','escalate',t.escalated?'toggled':'',''):''}${actions(t)}</div></div></header><div class="detail-layout"><div class="detail-left"><div class="detail-tabs" role="tablist" aria-label="Task details">${[['workspace',TYPES[t.type].tab,TYPES[t.type].icon],...(['document','request'].includes(t.type)?[]:[['overview','Overview','records']]),['activity','Activity','history'],['workflow','Workflow','flow']].map(([id,label,i])=>`<button class="detail-tab ${detail.tab===id?'active':''}" data-detail-tab="${id}" role="tab" aria-selected="${detail.tab===id}" aria-controls="detail-content">${icon(i,15)}${label}${id==='activity'?`<span class="tab-count">${t.activity.length}</span>`:''}</button>`).join('')}</div><div class="detail-main" id="detail-content" role="tabpanel">${mainContent(t)}</div></div><aside class="context-panel" aria-label="Persistent task context">${context(t)}</aside></div><footer class="detail-footer"><span>${icon('lock',13)}Visible to authorized staff</span><span>Updated ${date(t.updated,true)} EDT</span><span class="push"></span><span class="footer-key">Esc</span><span>to return to board</span></footer>`;
 dialog.querySelector('.detail-main').scrollTop=scroll;dialog.querySelector('.context-panel').scrollTop=contextScroll;if(dialog.querySelector('#review-table-scroll'))dialog.querySelector('#review-table-scroll').scrollTop=tableScroll;if(dialog.querySelector('#pdf-scroll'))dialog.querySelector('#pdf-scroll').scrollTop=pdfScroll;
}
export function renderMain(){const t=currentTask();if(!t)return;document.querySelector('#task-dialog').dataset.tab=detail.tab;document.querySelector('.detail-main').innerHTML=mainContent(t);document.querySelector('.detail-main').scrollTop=0;document.querySelectorAll('[data-detail-tab]').forEach(el=>{el.classList.toggle('active',el.dataset.detailTab===detail.tab);el.setAttribute('aria-selected',String(el.dataset.detailTab===detail.tab));});}
function mainContent(t){if(detail.tab==='workflow')return workflow(t);if(detail.tab==='activity')return activity(t);if(detail.tab==='overview')return overview(t);return t.type==='document'?documentWorkspace(t,detail):t.type==='outreach'?outreach(t):t.type==='payment'?payment(t):requestWorkspace(t);}
function context(t){
 const b=BOARDS[t.board],time=sla(t),s=stageFor(t);
 return `<div class="context-student"><button class="student-link" data-action="student-record">${avatar(t.student)}<div><div><strong>${esc(t.student)}</strong><span class="student-id">${t.studentId}</span></div><span>${esc(t.program)} · ${t.cohort}</span></div>${icon('external',13)}</button></div>
 <section class="context-section assignment-section"><div class="section-heading"><h3>Assignment</h3><span title="Responsible party for this stage">${s.owner==='EDgent'?'EDgent / staff':s.owner} owns stage</span></div><div class="assignment-person">${avatar(t.owner,'small')}<select aria-label="Task assignee" id="task-owner">${Object.entries(PEOPLE).map(([id,p])=>`<option value="${id}" ${id===t.owner?'selected':''}>${p.name}</option>`).join('')}</select></div><div class="assignment-team">${esc(t.team)}</div><select id="task-delegation" aria-label="Assignment mode">${['Direct assignment','Team queue','Specific team member','Automated agent'].map(x=>`<option ${t.delegation===x?'selected':''}>${x}</option>`).join('')}</select></section>
 <section class="context-section"><div class="section-heading"><h3>Stage ${s.owner==='EDgent'?'SLO':'SLA'}</h3>${badge(t.status==='completed'?'Met':time.remaining<0?'Breached':time.remaining<8?'Due soon':'On track',t.status==='completed'?'green':time.remaining<0?'red':time.remaining<8?'amber':'blue')}</div><div class="sla-remaining ${time.tone}">${icon('clock',16)}${time.label}</div><div class="sla-bar ${time.tone}"><i style="width:${time.percent}%"></i></div><dl class="context-fields"><dt>Due</dt><dd>${t.due?date(t.due,true)+' EDT':'Completed'}</dd><dt>Target / elapsed</dt><dd>${s.hours?s.hours+'h':'—'} / ${Math.max(0,Math.floor((Date.parse(store.clock)-Date.parse(t.entered))/3600000))}h</dd></dl></section>
 <section class="context-section"><h3>Linked requirement</h3><button class="requirement-link" data-action="requirement">${icon('checklist',17)}<span><strong>${t.requirements?.[0]?esc(t.requirements[0].title):t.type==='document'?esc(t.category==='Transcript'?'Final high school transcript':t.category):t.type==='payment'?'Fall 2026 account balance':t.type==='outreach'?'Complete orientation booking':'Enrollment readiness'}</strong><small>${t.requirements?.[0]?esc(t.requirements[0].status.replaceAll('_',' ')):t.status==='completed'?'Satisfied':t.status==='correction'?'Correction requested':'In progress'}</small></span>${icon('external',12)}</button></section>
 <section class="context-section"><dl class="context-fields"><dt>Priority</dt><dd><select id="task-priority" aria-label="Task priority">${['Urgent','High','Medium','Low'].map(x=>`<option ${t.priority===x?'selected':''}>${x}</option>`).join('')}</select></dd><dt>Source</dt><dd>${t.type==='outreach'?'Institution journey':t.type==='request'?'Student email / portal':'Student portal upload'}</dd><dt>Created</dt><dd>${date(t.created,true)}</dd><dt>Updated</dt><dd>${date(t.updated,true)}</dd></dl></section>
 <section class="context-section"><h3>Related work</h3>${store.tasks.filter(x=>x.studentId===t.studentId&&x.key!==t.key).slice(0,3).map(x=>`<button class="related-task" data-related-task="${x.key}">${typeIcon(x.type)}<span><strong>${x.key}</strong>${esc(x.title)}</span>${icon('chevron',12)}</button>`).join('')||'<span class="muted">No related open tasks</span>'}</section>`;
}
function overview(t){return `<div class="workspace-heading"><div><h2>Overview</h2><p>The purpose, context, and outcome of this work.</p></div></div><section class="content-section"><h3>Description</h3><p>${t.type==='document'?`Review ${esc(t.student)}’s ${esc(t.category.toLowerCase())} against the ${esc(BOARDS[t.board].space)} requirements for ${t.cohort}. EDgent extracts the document fields and flags potential differences; a staff reviewer makes the final decision.`:t.type==='outreach'?`Support ${esc(t.student)} with timely, personal outreach. Review the conversation, tailor the next message, and record the student’s next step.`:t.type==='payment'?`Reconcile the ${t.direction.toLowerCase()} payment of ${money(t.amount)} for ${esc(t.student)}. Track the settlement trail and any outstanding balance.`:esc(t.requestText||`${t.student} submitted a request through the student portal. Review the context, coordinate with the appropriate team, and record a clear resolution.`)}</p></section><section class="content-section"><h3>Acceptance criteria</h3><ul class="criteria">${(t.type==='document'?['Student identity matches the application record','Document meets the linked checklist requirement','All exceptions have a recorded staff decision','Student is informed of the result']:t.type==='outreach'?['Message is personal, accurate, and approved','Communication attempts and replies are recorded','Student’s next step is clear']:t.type==='payment'?['Amount and direction match the account record','Transactions are reconciled','Remaining balance is accounted for']:['Request and supporting information are reviewed','Appropriate team has provided input','Resolution is recorded and available to the student']).map(x=>`<li>${icon(t.status==='completed'?'check':'circle',16,t.status==='completed'?'green':'muted')}${x}</li>`).join('')}</ul></section><section class="content-section"><h3>Labels</h3><div class="label-list">${t.labels.map(l=>badge(l)).join('')}${badge(t.cohort)}</div></section>${t.decision||t.resolution?`<section class="content-section"><h3>Recorded outcome</h3><div class="review-banner green">${icon('check',20)}<p>${esc(t.decision||t.resolution)}</p></div></section>`:''}<section class="content-section"><h3>Latest update</h3><div class="overview-update">${avatar(t.activity.at(-1).who==='student'?t.student:t.activity.at(-1).who)}<div><strong>${date(t.activity.at(-1).at,true)}</strong><p>${esc(t.activity.at(-1).text)}</p></div></div></section>`;}
function activity(t){
 const events=[...t.activity].sort((a,b)=>Date.parse(b.at)-Date.parse(a.at)).filter(e=>detail.activity==='all'||e.kind===detail.activity);
 return `<div class="activity-filters" role="tablist" aria-label="Activity filter">${[['all','All'],['comment','Internal comments'],['communication','Communications'],['history','History']].map(([id,label])=>`<button data-activity="${id}" role="tab" aria-selected="${detail.activity===id}" class="activity-filter ${detail.activity===id?'active':''}">${label}</button>`).join('')}<span class="push"></span><span class="muted">${events.length} events · Newest first</span></div>
 ${['all','comment'].includes(detail.activity)?`<form id="comment-form" class="comment-form">${avatar('ML')}<div class="comment-input-wrap"><textarea name="comment" maxlength="500" aria-label="Write a comment" placeholder="Add an internal comment…" required rows="1">${esc(detail.commentDrafts[t.key]||'')}</textarea><div class="comment-controls"><span>${icon('lock',12)}Private · staff only</span><button class="btn primary" type="submit">Post comment</button></div></div></form>`:''}
 ${t.workItemId&&['all','communication'].includes(detail.activity)?conversation(t):''}
 <div class="activity-stream">${events.map(e=>`<article class="activity-event ${e.kind==='comment'?'is-comment':''} ${e.kind==='communication'?'is-communication':''}"><div class="event-avatar">${e.kind==='comment'?avatar(e.who==='student'?t.student:e.who):`<span class="event-icon">${icon(e.kind==='communication'?'mail':e.who==='EG'?'robot':e.who==='student'?'upload':'history',15)}</span>`}</div><div class="event-body"><div class="event-meta"><strong>${e.who==='student'?esc(t.student):PEOPLE[e.who]?.name||esc(e.who)}</strong>${e.kind==='communication'?badge(e.channel||'Student communication','blue'):e.kind==='comment'?badge('Internal · private'):e.who==='EG'?badge('System','purple'):''}<time>${date(e.at,true)}</time></div><p>${esc(e.text)}</p></div></article>`).join('')||'<div class="board-empty">No events in this category yet.</div>'}</div>`;
}
function outreach(t){if(t.workItemId)return `<div class="workspace-heading"><div><h2>Communication workspace</h2><p>Personal outreach, with the full conversation in view.</p></div></div>${conversation(t)}`;const sent=['waiting','responded','completed'].includes(t.status);return `<div class="workspace-heading"><div><h2>Communication workspace</h2><p>Personal outreach, with the full conversation in view.</p></div>${badge(t.delegation==='Automated agent'?'EDgent assisted':'Staff led','purple')}</div><div class="outreach-goal"><span>${icon('flag',21)}</span><div><strong>${esc(t.title)}</strong><p>${t.board==='fa-outreach'?'Help the student understand and complete their financial aid next step.':'Help the student choose an orientation session that works with their schedule.'}</p></div></div><div class="communication-stats"><div><span>Channel</span><strong>${icon('message',17)}Student portal</strong></div><div><span>Attempts</span><strong>${sent?t.messages.filter(m=>m.who!=='student').length:0}</strong></div><div><span>Last response</span><strong>${t.messages.some(m=>m.who==='student')?'Today · 11:00 AM':'Awaiting reply'}</strong></div></div><section class="content-section"><div class="section-heading"><h3>Conversation</h3><span>${esc(t.student)}</span></div><div class="conversation">${(sent?t.messages:[]).map(m=>`<div class="message-bubble ${m.who==='student'?'incoming':'outgoing'}"><div>${avatar(m.who==='student'?t.student:m.who,'small')}<strong>${m.who==='student'?esc(t.student):PEOPLE[m.who].name}</strong><time>${date(m.at,true)}</time></div><p>${esc(m.text)}</p><small>${m.channel}</small></div>`).join('')||'<p class="muted">No messages sent yet. Prepare and submit an outreach draft below.</p>'}</div></section>${t.status!=='completed'?`<form id="outreach-form" class="content-section"><div class="section-heading"><h3>${t.status==='responded'?'Your reply':'Message draft'}</h3>${badge('Portal message','purple')}</div><label class="form-field"><span>Subject</span><input name="subject" aria-label="Message subject" value="${esc(t.subject||t.title)}" required></label><label class="form-field"><span>Message</span><textarea name="message" aria-label="Outreach message" rows="5" required>${esc(t.draft||(t.status==='responded'?`Hi ${t.student.split(' ')[0]}, absolutely — our Thursday session begins at 3 PM. You can book it under Enrollment → Orientation in your student portal. Let me know if you’d like a hand!`:`Hi ${t.student.split(' ')[0]}, I’m checking in about your next enrollment step. We’re here to help you get ready for the fall. Is there anything we can help you work through?`))}</textarea></label><div class="composer-footer"><span class="muted">${icon('shield',14)}${t.status==='responded'?'Staff reply · mock delivery':'Approval required before delivery'}</span><button class="btn ${t.status==='responded'?'primary':''}" type="submit">${icon(t.status==='responded'?'send':'check',15)}${t.status==='responded'?'Send mock reply':'Save draft'}</button></div></form>`:`<div class="review-banner green">${icon('check',20)}<strong>Outreach completed. Conversation retained with the student record.</strong></div>`}`;}
function payment(t){return `<div class="workspace-heading"><div><h2>Payment workspace</h2><p>Account balance, payment direction, and settlement evidence.</p></div>${badge(t.direction==='Inbound'?'Inbound · student to institution':'Outbound · institution to student',t.direction==='Inbound'?'blue':'purple')}</div>${t.status==='exception'?`<div class="review-banner amber">${icon('alert',21)}<div><strong>${t.direction==='Outbound'?'Refund needs reconciliation':'Payment did not settle'}</strong><p>${t.direction==='Outbound'?'The original payment and refund references need to be matched before release.':'The processor declined the payment. Confirm payment details with the student before retrying.'}</p></div></div>`:''}<div class="payment-summary"><div><span>Total ${t.direction==='Outbound'?'refund':'due'}</span><strong>${money(t.amount)}</strong></div><div><span>${t.direction==='Outbound'?'Refunded':'Settled'}</span><strong class="green">${money(t.paid)}</strong></div><div><span>Remaining balance</span><strong>${money(t.amount-t.paid)}</strong></div></div><div class="payment-progress"><i style="width:${t.paid/t.amount*100}%"></i></div><div class="section-heading payment-plan-heading"><h3>${t.amount>1000?'Payment schedule':'Payment details'}</h3><span>${t.cohort} · USD</span></div>${t.amount>1000?`<div class="installments">${[1,2,3,4].map((n)=>`<div class="installment ${t.paid>=t.amount/4*n?'settled':''}"><div>${icon(t.paid>=t.amount/4*n?'check':'calendar',17)}<strong>Installment ${n}</strong></div><span>${['Sep 1','Oct 1','Nov 1','Dec 1'][n-1]}, 2026</span><strong>${money(t.amount/4)}</strong>${badge(t.paid>=t.amount/4*n?'Settled':'Scheduled',t.paid>=t.amount/4*n?'green':'gray')}</div>`).join('')}</div>`:`<div class="payment-reference"><span>Reference <strong>${t.key}-2026</strong></span><span>Method <strong>ACH transfer</strong></span><span>Account <strong>•••• 4821</strong></span></div>`}<section class="content-section"><div class="section-heading"><h3>Transactions</h3>${badge(t.transactions.length+' recorded')}</div><table class="transactions"><thead><tr><th>Reference / date</th><th>Method</th><th>Amount</th><th>Status</th></tr></thead><tbody>${t.transactions.map(tx=>`<tr><td><strong>${tx.id}</strong><small>${tx.date}</small></td><td>${tx.method}</td><td>${money(tx.amount)}</td><td>${badge(tx.status,'green')}</td></tr>`).join('')||'<tr><td colspan="4" class="muted">No confirmed settlements recorded.</td></tr>'}</tbody></table></section><div class="review-banner blue">${icon('info',20)}<div><strong>Balance-aware workflow</strong><p>A partial settlement keeps this task open. The task completes only when the remaining ${t.direction==='Outbound'?'refund':'balance'} is settled.</p></div></div>`;}
