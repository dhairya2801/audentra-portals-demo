# Temporary contract snapshot

This is a vendored snapshot of `packages/contracts` from Audentra Platform so
the portals repository can install and validate independently during the
initial split. Do not evolve this copy independently.

Replace it with a compiled, versioned `@audentra/api-client` generated and
published by the platform repository before separate feature work begins.
